window.dxSample = $.extend(true, window.dxSample, {
  "config": {
    "layoutSet": "split",
    "navigation": [
      {
        "title": "Home",
        "action": "#home",
        "icon": "home"
      },
      {
        "title": "About",
        "action": "#about",
        "icon": "info"
      }
    ]
  }
});